@layer base {
  input,
  textarea,
  select {
    color: var(--text);
    border: var(--border-color) 1px solid;
    background-color: var(--bg-secondary);
    padding: 1rem;
    @apply rounded;
  }
}
